import 'package:flutter/material.dart';

@immutable
final class InformationSheetTemplate extends StatelessWidget {
  const InformationSheetTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      mainAxisSize: MainAxisSize.min,
    );
  }
}
